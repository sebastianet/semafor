
     i = Math.random() ;   // generate a (floating point) random number between [0 and 1)
     console.log( 'I = [' + i + '].' ) ;

     j = Math.floor( i * 10 ) ; // integer value
     console.log( 'J = [' + j + '].' ) ;
